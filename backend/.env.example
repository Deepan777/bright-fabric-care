# Copy to .env for local development.
# On Render.com these are set in the dashboard (DATABASE_URL is auto-provided
# when you attach the PostgreSQL instance).
DATABASE_URL=postgresql://user:password@localhost:5432/bright_fabric_care
PORT=4000
NODE_ENV=development
